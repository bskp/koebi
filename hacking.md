Extending Micropython Code
==========================

TODO

- open ``firmware/``in VS Code
- install the Pymakr extension for easy REPLing and file synchronization
- install micropy für intellisense

- remote.py: Abstracts a parameter (and input/outut maps) and pushes value updates (and _only_ the updates) to the TouchOSC client
